

## Sprint 8 — финальная косметика (опционально)

Из трёх «долгов» имеет смысл закрыть только **один** прямо сейчас. Остальные два — оставить как есть до отдельного решения через 1–2 месяца.

### Делаем сейчас (5 минут работы)

**HistoryChart.tsx — обновить легенду графика истории**

- Убрать линии `schema` и `direct` (устаревшие концепции)
- Переименовать `ai` → `geo` (визуально, в legend), оставить тот же ключ данных
- Итого 3 линии: **Total** (жирная) / **GEO** / **SEO** / **CRO**
- Цвета: Total — primary, GEO — фиолетовый, SEO — циан, CRO — зелёный
- Старые сканы из истории продолжат отображаться корректно (worker уже пишет нужные ключи)

```text
Было:  [Total] [SEO] [AI] [Schema] [Direct]    — 5 линий, путаница
Стало: [Total] [GEO] [SEO] [CRO]               — 3 линии + total, чисто
```

### НЕ делаем сейчас — оставляем как есть

**`siteCheck.ts:59-66` (ALTER TABLE legacy-колонок)**
- Зачем: страховка отката на старых сканах в проде
- Когда чистить: через 1–2 месяца отдельной миграцией с архивацией старых записей
- Риск удаления сейчас: можно сломать чтение старых сканов в истории

**`SiteCheckPipeline.ts` 2335 строк (монолит)**
- Зачем: рефакторинг «по живому» — высокий риск поломать pipeline сразу после релиза
- Когда чистить: отдельным спринтом после 1–2 недель стабильной работы в проде
- Объём: 4–6 часов, 15+ новых файлов, нужен полный регресс-тест

### Технические детали

**Файл:** `src/components/site-check/HistoryChart.tsx`

Изменения:
1. `SERIES`-массив — заменить с 5 элементов на 4: `total`, `geo`, `seo`, `cro`
2. В `data.map` — `geo: h.ai ?? 0` (используем существующий ключ `ai` как источник, т.к. worker туда пишет geoScore), `cro: h.direct ?? 0` (worker пишет croScore в `direct`)
3. Если в API `getDomainHistory` есть отдельные поля `geo`/`cro` — использовать их напрямую; если нет — оставить mapping через `ai`/`direct`

После правки: задеплоится автоматически через GitHub Actions (как фронт, так и бэк не меняем — только фронт).

