

## Обновление страницы «Проверка сайта» — список проверок и layout

### Проблемы
1. Список «Что проверяем» не отражает новые проверки (Direct Readiness, AI-генерация объявлений, Yandex Metrika, viewport)
2. На десктопе 8 пунктов идут одним столбиком — выглядит растянуто и некомпактно

### Изменения в `src/pages/SiteCheck.tsx`

**1. Обновить `checkItems` — актуальный список из 10 пунктов:**

```typescript
const checkItems = [
  "SEO Score (20+ параметров)",
  "LLM Score (AI-готовность)",
  "Direct Readiness Score",
  "AI-генерация объявления Директа",
  "Топ-10 конкурентов",
  "200+ ключевых слов",
  "Минус-слова для Директа",
  "E-E-A-T и Schema.org",
  "llms.txt проверка и генерация",
  "Экспорт PDF / Word / CSV",
];
```

**2. Сделать сетку 2 колонки на десктопе:**

Заменить `<ul className="space-y-3">` на `<ul className="grid grid-cols-1 sm:grid-cols-2 gap-3">` — на мобиле остаётся 1 колонка, на десктопе — аккуратные 2 столбца.

**3. Расширить контейнер** с `max-w-2xl` до `max-w-3xl`, чтобы две колонки не были сжаты.

