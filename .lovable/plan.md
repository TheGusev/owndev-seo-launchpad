

## Plan: Remove AboutSection

**DELETE reference** in `src/pages/Index.tsx` — remove `AboutSection` import and `<AboutSection />` from JSX.

**DELETE file** `src/components/AboutSection.tsx`.

2 changes, straightforward removal.

