

## Порог GEO Рейтинга: 70 → 90

Сейчас в `GeoRatingNomination.tsx` строка 41: `if (totalScore < 70 || sent) return null;` — баннер "Добавить в рейтинг" показывается от 70 баллов.

### Изменения

**Файл:** `src/components/site-check/GeoRatingNomination.tsx`

1. Строка 41: заменить `70` на `90`
2. Строка 76: обновить текст с `{totalScore}/100 — это уровень ТОП-рейтинга!` → актуализировать под новый порог

Одно число, один файл, ничего больше не трогаем.

